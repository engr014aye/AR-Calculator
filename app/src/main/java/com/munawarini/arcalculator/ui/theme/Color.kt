package com.munawarini.arcalculator.ui.theme

import androidx.compose.ui.graphics.Color

// ── Deep Space Dark Palette ───────────────────────────────────────────────────
val DarkBackground      = Color(0xFF0A0A0F)
val DarkSurface         = Color(0xFF161622)
val DarkSurfaceVariant  = Color(0xFF1E1E30)
val DarkSurfaceHigh     = Color(0xFF252540)

// ── Light Palette ─────────────────────────────────────────────────────────────
val LightBackground     = Color(0xFFF0F2F8)
val LightSurface        = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFEEEEFA)
val LightSurfaceHigh    = Color(0xFFE0E0F5)

// ── Glass overlays ────────────────────────────────────────────────────────────
val GlassDark           = Color(0x12FFFFFF)   // 7% white
val GlassDarkBorder     = Color(0x1FFFFFFF)   // 12% white stroke
val GlassLight          = Color(0x10000000)   // 6% black
val GlassLightBorder    = Color(0x18000000)   // 9% black stroke

// ── Accent Palette ────────────────────────────────────────────────────────────
val AccentViolet        = Color(0xFF7C5CFF)   // Primary — neon violet
val AccentVioletLight   = Color(0xFF5B3FE4)   // Primary light mode
val AccentCyan          = Color(0xFF00D4FF)   // Secondary accent
val AccentCyanLight     = Color(0xFF0099CC)

// Gradient pair used for Equals button and active states
val GradientStart       = Color(0xFF7C5CFF)
val GradientEnd         = Color(0xFF00D4FF)

// ── Button Background Tokens ──────────────────────────────────────────────────
// Dark
val DarkNumberBtn       = Color(0xFF1E1E30)
val DarkOperatorBtn     = Color(0xFF2A2A40)
val DarkFunctionBtn     = Color(0xFF141428)
val DarkClearBtn        = Color(0xFF3A1A2A)

// Light
val LightNumberBtn      = Color(0xFFF5F5FF)
val LightOperatorBtn    = Color(0xFFE8EAFB)
val LightFunctionBtn    = Color(0xFFDDDDF5)
val LightClearBtn       = Color(0xFFFFE8EE)

// ── Text ──────────────────────────────────────────────────────────────────────
val TextWhite           = Color(0xFFFFFFFF)
val TextSecondary       = Color(0xFFB0B0CC)
val TextMuted           = Color(0xFF6B6B8A)
val TextDark            = Color(0xFF111120)
val TextDarkSecondary   = Color(0xFF5A5A80)

// ── Semantic ──────────────────────────────────────────────────────────────────
val ErrorRed            = Color(0xFFFF6B6B)
val SuccessGreen        = Color(0xFF4ECDC4)
val WarningAmber        = Color(0xFFFFB347)
